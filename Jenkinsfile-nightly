pipeline {
    agent any

    stages {

        stage('Docker'){
            agent any
            steps{
                sh '''
                    docker build --network=host -t my-playwright .
                '''
            }
        }
        
    }
}